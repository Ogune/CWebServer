```js
Const CWebServ = {
  "description": "A basic web server in C.",
  "?": "Handles GET requests with basic protections.",
  "Usage": "cd CWebServ && make && ./srv"
}
```

```javascript
const http = {
  "description": "A custom HTTP server implementation in C++98",
  "features": ["HTTP/1.1", "Non-blocking I/O", "CGI"],
  "usage": "cd httpd && make && ./webserv",
}
```
